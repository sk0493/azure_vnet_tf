# Example of Terraform Module with Submodule

The submodule is used to create a virtual network on Azure cloud.

The following variables are needed:
- vnet_name
- vnet_resource_group_name
- vnet_location